# backend/providers/__init__.py

"""
FlowSync provider helper package.

These files are standalone helpers for real map/geocoding/routing providers.
They are not wired into the main backend API yet.

Member 2 can later connect these helpers safely into:
- GET /api/locations/search
- POST /api/routes/recommend
"""
