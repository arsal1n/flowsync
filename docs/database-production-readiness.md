# FlowSync Database Production Readiness

FlowSync Backend v1 now includes database production-readiness support.

## Current Database Mode

The backend currently uses SQLite by default for local development and demo.

```text
backend/flowsync.db